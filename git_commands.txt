COLLEGARE

git init
ssh-keygen -t rsa

cd.ssh
cat id_rsa.pub

copio e incollo su git->impostazioni->new ssh key

git config --global user.name "MichBattle"
git config --global user.email "michele.calliari04@gmail.com"

git clone https://github.com/MichBattle/univerona.git

cd univerona

PUSH
git add .
git commit -m "commento"
git push origin main

PULL
git checkout main //solo per controllare se pullo la roba giusta
git pull origin main